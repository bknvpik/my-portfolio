import React from 'react';
import arrow from '../arrow.svg';

const Footer = () => {

    return (
        <footer>
            <img src={arrow} alt=""></img>
        </footer>
    );
};

export default Footer;