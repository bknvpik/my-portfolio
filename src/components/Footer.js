import React from 'react';
import arrow from '../images/arrow.svg';

const Footer = () => {

    return (
        <footer>
            <img src={arrow} alt=""></img>
        </footer>
    );
};

export default Footer;